<h1> <%= @genre.name %></h1>

<ol>
    <%  @genre.artists.each do |artist|%>
    <li><%= artist.name %></li>
    <% end %>
</ol>

<br><br>

<ol>
    <%  @genre.songs.each do |song|%>
    <li><%= song.name %></li>
    <% end %>
</ol>
